# Unsloth GPT-OSS-20B Requirements
# Python 3.10+ required

# Core Unsloth packages (install with special command)
# pip install --upgrade --force-reinstall --no-cache-dir unsloth unsloth_zoo

# Additional dependencies (install with --no-deps to avoid conflicts)
# pip install --no-deps trl peft accelerate bitsandbytes

# Standard dependencies
torch>=2.4.0
transformers>=4.44.0
datasets>=2.14.0
huggingface-hub>=0.24.0
tokenizers>=0.19.0

# Training utilities
tensorboard>=2.15.0
wandb>=0.16.0

# Optimization
xformers>=0.0.27
triton>=3.0.0

# Utilities
tqdm>=4.66.0
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Evaluation
rouge-score>=0.1.2
nltk>=3.8.0

# Export
sentencepiece>=0.2.0
protobuf>=4.25.0

# Development
ipython>=8.12.0
jupyter>=1.0.0
black>=23.0.0
pylint>=3.0.0